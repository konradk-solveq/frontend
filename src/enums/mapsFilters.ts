export enum dateFilter {
    latest,
    oldest,
}

export enum routeTypeFilter {
    all,
    nonPublic,
    public,
}

export enum levelFilter {
    easy,
    medium,
    hard,
}

export enum pavementFilter {
    gravel,
    asphalt,
    paved,
    unsealed,
    bike,
}

export enum tagsFilter {
    observation,
    smallTraffic,
    weekend,
    attraction,
    goodFood,
    forKids,
}

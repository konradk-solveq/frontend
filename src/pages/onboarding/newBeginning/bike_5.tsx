import React from 'react';
import Svg, {G, Path, Circle, Ellipse} from 'react-native-svg';

interface Props {
    style: any;
}

const Bike_5: React.FC<Props> = (props: Props) => ();

export default Bike_5;

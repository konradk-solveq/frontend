import {BasicCoordsType} from '@type/coords';

/**
 * Default location for map views - the center of Warsaw
 */

export const defaultLocation: BasicCoordsType = {
    latitude: 52.229873,
    longitude: 21.011688,
};
